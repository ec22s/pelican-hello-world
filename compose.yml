services:
  py:
    build:
      context: .
    ports:
      - "8000:8000"
    tty: true
    volumes:
      - pelican-project:/project
      - ./project/content:/project/content
      - ./project/pelicanconf.py:/project/pelicanconf.py

volumes:
  pelican-project:
